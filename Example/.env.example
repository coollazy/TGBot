# 複製這個檔案成 .env（同一個資料夾底下），把值換成你自己跟 @BotFather 申請的 token：
#
#   cp .env.example .env
#
# .env 已經在 .gitignore／.dockerignore 裡被排除，不會被 commit、也不會被打包進
# docker image——docker-compose.yml 用 env_file 在容器啟動時才把這個值帶進去。
TGBOT_TOKEN=123456:AAAA-your-bot-token-from-BotFather
